# Optional standalone HTTP host only. Not loaded automatically.
TOOLS_HOST=127.0.0.1
TOOLS_PORT=8787
TOOLS_ALLOWED_DIRECTORIES=["/absolute/path/to/project"]
# Set a freshly generated secret outside the repository; minimum 32 characters.
TOOLS_TOKEN=
