{
  "name": "macos",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "workspaces": ["shared", "window", "screen", "chrome"],
  "scripts": {
    "dev": "bun run --filter '@meta/window' --filter '@meta/screen' --filter '@meta/chrome' dev"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
