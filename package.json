{
  "name": "macos",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "workspaces": ["shared", "window", "screen", "chrome", "android", "input"],
  "scripts": {
    "dev": "bun run --filter '@meta/window' --filter '@meta/screen' --filter '@meta/chrome' --filter '@meta/android' --filter '@meta/input' dev"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
